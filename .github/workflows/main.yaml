name: "Basic"

on:
  push:
    branches:
      - main
jobs:
  build-test:
    name: "Build Test Job"

    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        shell: bash
        run: |
          echo "Hello World"
  dev:
    name: "Dev Job"
    needs:
      - build-test

    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        shell: bash
        run: |
          echo "Hello World"
  test_1:
    name: "Test_1 Job"
    needs:
      - dev

    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        shell: bash
        run: |
          echo "Hello World"
  test_2:
    name: "Test_2 Job"
    needs:
      - test_1

    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        shell: bash
        run: |
          echo "Hello World"
  prod:
    name: "Production Job"
    needs:
      - test_2

    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        shell: bash
        run: |
          echo "Hello World"